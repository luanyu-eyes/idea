spring:
  datasource:  # 配置mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/work?user=root&password=123456&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
    
server:
  port: 8080  # 默认端口
  
mybatis:
  mapper-locations: classpath:mapper/*.xml 

